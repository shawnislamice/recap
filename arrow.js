// function add(x, y) {
//   return x + y;
// }

const add = (x, y) => {
  return x + y;
};
console.log(add(2, 3));

// Arrow function returns implicit return

const sub = (a, b) => a + b;
console.log(sub(5, 5));

const double = (x) => x * 2;
console.log(double(5));

// Arrow function hoisting ignore kore 